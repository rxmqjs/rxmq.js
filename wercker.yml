# The container definition we want to use for developing our app
box: nodesource/trusty

dev:
  steps:
    - npm-install
    - npm-test

build:
  steps:
    - npm-install
    - npm-test
    - script:
      code: npm run docs

deploy:
  steps:
    - lukevivier/gh-pages:
      token: $GH_TOKEN
      basedir: esdoc
